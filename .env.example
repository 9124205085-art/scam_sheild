# Optional. The app is fully usable without this — rules still score, explain, and highlight.
# Get a key at https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=

# Used by /api/gemini when you deploy to Vercel (keeps the key off the client).
GEMINI_API_KEY=
